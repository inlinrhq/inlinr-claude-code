{
  "name": "@inlinr/claude-code",
  "version": "0.4.0",
  "private": true,
  "description": "Track time, tokens and AI-written lines from Claude Code",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --target=node --outfile=dist/index.js",
    "test": "bun test",
    "check": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
